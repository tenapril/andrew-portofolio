/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com', 'upload.wikimedia.org', 'play-lh.googleusercontent.com', 'd3g5ywftkpzr0e.cloudfront.net', 'xeratic.com'],
  },
};

export default nextConfig;
